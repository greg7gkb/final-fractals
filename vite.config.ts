import { defineConfig } from 'vite';

export default defineConfig({
  // Serve from root so index.html is found automatically.
  // For GitHub Pages deployment, set base to your repo name:
  //   base: '/final-fractals/',
  base: './',
});
